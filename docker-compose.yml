services:
  web:
    build: .
    expose:
      - "8000"          # หรือจะไม่ใส่ก็ได้ ถ้าไปตั้งใน Coolify แล้ว
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FLASK_ENV=production
      - PORT=8000
    restart: always
